{
  "name": "mustafa-portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp -r css dist/ && cp -r js dist/"
  },
  "description": "Personal Portfolio Code"
}
